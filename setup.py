#create ssh-key pair
#push pub key to GIN account and log in the user with a personal access token
#save the token and private key both as a docker secrets

#check whether drone is configured and running
#if not --> spawn a drone container

#continue with the workflow automation
